Neste arquivo descreve-se os passos a seguir para criar, o código de tratamento (_trata_mento.py). Esse arquivo espelha como o
algoritmo em _trata_mento.py deve funcionar.

# Objetivo de algoritmo

O algoritmo de tratamento tem como objetivo final gerar a base dados necessária com todas as palavras contidas no arquivo de pala-
vras fornecido como entradas ao algoritmo do _trata_mento.py

## Como o algoritmo deve funcionar

* Receber como o input o nome do arquivo de onde vai ser extraido as palavras para a base dados dessa língua
* Percorrer linha por linha deste das palavras
* Preencher a base dados com as correspondentes palavras novas a acrescentar